# 📋 Информация о проекте Clean Metadata

## ✅ Статус установки

**Проект полностью настроен и готов к использованию!**

---

## 📦 Установленные компоненты

### Python пакеты (установлены в venv)

| Пакет | Версия | Назначение |
|-------|--------|-----------|
| **Pillow** | 11.3.0 | Обработка изображений (обязательно) |
| **numpy** | 2.0.2 | Численные операции для watermark-атак (обязательно) |
| **scipy** | 1.13.1 | DCT и геометрические атаки |
| **PyWavelets** | 1.6.0 | Атаки в вейвлет-домене (DWT) |
| **mutagen** | 1.47.0 | Удаление метаданных из аудио |

### Внешние инструменты (требуют отдельной установки)

| Инструмент | Статус | Команда установки (macOS) | Назначение |
|-----------|--------|---------------------------|-----------|
| **exiftool** | ❌ Не установлен | `brew install exiftool` | Полная очистка метаданных |
| **ffmpeg** | ❌ Не установлен | `brew install ffmpeg` | Обработка видео |

> **Примечание:** Внешние инструменты опциональны. Скрипт работает без них, но с ограниченным функционалом для видео и метаданных.

---

## 🗂️ Структура проекта

```
clean-metadata/
│
├── venv/                       # ✅ Виртуальное окружение Python 3.9
│   ├── bin/                    # Исполняемые файлы (python, pip)
│   └── lib/                    # Установленные пакеты
│
├── clean_metadata.py           # ✅ Основной скрипт (скопирован из Downloads)
├── requirements.txt            # ✅ Список зависимостей
├── requirements-lock.txt       # ✅ Зафиксированные версии пакетов
│
├── README.md                   # ✅ Полная документация проекта
├── QUICKSTART.md              # ✅ Быстрый старт и основные команды
├── PROJECT_INFO.md            # ✅ Этот файл
│
├── .gitignore                 # ✅ Исключения для Git
│
├── examples/                  # 📁 Папка для входных файлов
│   └── .gitkeep               # ✅ Placeholder файл
│
└── cleaned/                   # 📁 Папка для выходных файлов
    └── .gitkeep               # ✅ Placeholder файл
```

---

## 🚀 Как начать работу

### 1. Активация виртуального окружения

```bash
cd "/Users/kkolesss/Documents/VSCode Projects/clean-metadata"
source venv/bin/activate
```

После активации в терминале появится `(venv)`.

### 2. Проверка установки

```bash
python clean_metadata.py --check-deps
```

**Ожидаемый результат:**
```
Python пакеты:
  ✓ Pillow       — изображения (обязательно)
  ✓ numpy        — атаки на watermark (обязательно)
  ✓ scipy        — DCT и geometric атаки
  ✓ PyWavelets   — wavelet атака
  ✓ mutagen      — аудио

Внешние инструменты:
  ✗ ffmpeg       — видео
  ✗ exiftool     — полная очистка метаданных
```

### 3. Первый запуск

```bash
# Поместите тестовое изображение в папку examples/
# Затем выполните:
python clean_metadata.py examples/photo.jpg
```

Результат появится в папке `cleaned/photo_clean.jpg`

---

## 📝 Основные команды

### Простая обработка

```bash
python clean_metadata.py photo.jpg
```

### С детальным анализом

```bash
python clean_metadata.py photo.jpg --analyze -v
```

### Обработка всей папки

```bash
python clean_metadata.py ./examples --wm-method ensemble --wm-strength 0.5
```

### Только метаданные (без watermark-атаки)

```bash
python clean_metadata.py photo.jpg --no-watermark-attack
```

---

## 🔧 Техническая информация

### Версия Python

```bash
./venv/bin/python --version
```

Используется: **Python 3.9** (системная версия)

### Размер виртуального окружения

~30-40 MB (все зависимости установлены)

### Путь к проекту

```
/Users/kkolesss/Documents/VSCode Projects/clean-metadata
```

---

## 📚 Дополнительная документация

- **[README.md](README.md)** - Полная документация с описанием всех возможностей
- **[QUICKSTART.md](QUICKSTART.md)** - Быстрый старт и шпаргалка по командам

---

## 🔄 Обновление зависимостей

Если потребуется обновить пакеты:

```bash
source venv/bin/activate
pip install --upgrade -r requirements.txt
pip freeze > requirements-lock.txt
```

---

## 🗑️ Деактивация окружения

Когда закончите работу:

```bash
deactivate
```

---

## 💡 Советы

1. **Всегда активируйте venv** перед запуском скрипта
2. **Используйте `--analyze`** для понимания эффективности атак
3. **Начните с `--wm-strength 0.5`** для баланса качество/эффективность
4. **Установите exiftool и ffmpeg** для полного функционала
5. **Используйте `ensemble` метод** для максимальной эффективности

---

## ❓ Поддержка

При возникновении проблем проверьте:

1. Активировано ли виртуальное окружение: `(venv)` в начале строки
2. Установлены ли зависимости: `python clean_metadata.py --check-deps`
3. Используется ли правильный Python: `which python` должен показать путь в venv

---

**Дата создания:** 21 февраля 2026  
**Python версия:** 3.9  
**Статус:** ✅ Готов к использованию
